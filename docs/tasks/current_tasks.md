# ZigCel Project Tasks

## 1. 計画と要件定義
- [x] 方向性の決定（ブラウザ動作のWASMスプレッドシート）
- [x] フレームワーク非依存のUI提供方針の決定（Web Components / Vanilla JS）

## 2. プロジェクト初期セットアップ
- [x] OSS向けREADME.mdの作成
- [x] Docker (Docker Compose) による開発環境の構築
- [x] Zigプロジェクトの初期化 (`build.zig`の設定)
- [x] フロントエンド開発環境の構築（Vite + TypeScript）
- [x] ZigをWASMにビルドし、JSから呼び出す最小構成の実装

## 3. コアエンジン開発 (Zig/WASM)
- [x] メモリアロケーションとJS/WASM間のデータ受け渡し機構
- [x] 基本的なスプレッドシートのデータ構造（行・列・セルの管理）
- [x] 簡単な計算ロジック（数値の加算など）

## 4. UIレンダリング基盤 (TypeScript/Canvas)
- [x] `<zig-cel>` カスタムタグ（Web Component）のスケルトン作成とテストマウント
- [x] Canvasコンテキストの初期化とリサイズ監視処理（ResizeObserver）の設定
- [x] 固定サイズのマス目（グリッド線）を描画するロジックの実装
- [x] セルのヘッダー（A, B, C... 1, 2, 3...）を描画するロジックの実装
- [x] スクロールに応じた描画オフセット計算の実装

## 5. インタラクション実装
- [x] セルの選択とハイライト
- [x] データ入力ダイアログ/インプット要素の配置
- [x] UIからの入力データをWASMに反映、再計算結果をUIに反映

## 6. 各環境での動作検証（デモアプリ作成）
- [ ] Vanilla HTML (ブラウザ直接読み込み) でのデモ
- [ ] Reactアプリ上での動作デモ
- [ ] (オプション) Railsチックな静的HTMLへのマウントデモ
